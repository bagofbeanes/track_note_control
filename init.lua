require "util"
DEFAULT_TIME = -3000
START_TIME = -2001

-- MAX_LANE_COUNT is recommended to be set to an Even Integer in order for things to work as expected
MAX_LANE_COUNT = 32

require "track.setup"
require "track.controls"

require "note.setup"
require "note.controls"